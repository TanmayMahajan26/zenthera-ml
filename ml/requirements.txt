# Zenthera ML — Python Dependencies
# ==================================
# For data fetching (fetch_genomic_data.py): no extra deps needed (uses stdlib)
# For model training (antibiotic_resistance_predictor.py):
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0
joblib>=1.3.0
